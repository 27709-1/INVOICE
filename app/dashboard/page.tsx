async function report(){const base=process.env.NEXTAUTH_URL??'http://localhost:3000';try{return await fetch(`${base}/api/reports/dashboard`,{cache:'no-store'}).then(r=>r.json())}catch{return{summary:{}}}}
export default async function Dashboard(){const {summary:s}=await report();const cards=['netSales','totalPurchases','cogs','grossProfit','grossMargin','totalExpenses','netProfit','netMargin','receivables','payables','inventoryValue','lowStock','outOfStock','overstock'];return <div className="space-y-6"><h1 className="text-3xl font-bold">Dashboard</h1><div className="grid gap-4 md:grid-cols-3 xl:grid-cols-4">{cards.map(c=><div className="card" key={c}><div className="text-sm text-slate-500">{c}</div><div className="text-2xl font-bold">{Number(s?.[c]??0).toFixed(c.includes('Margin')||c.includes('margin')?2:0)}</div></div>)}</div><div className="card"><h2 className="font-bold">Quick Actions</h2><div className="mt-3 flex flex-wrap gap-2">{['New Invoice','New Purchase','Add Product','Add Customer','Add Supplier','Record Payment','Add Expense','Stock Adjustment'].map(x=><button className="btn" key={x}>{x}</button>)}</div></div></div>}
